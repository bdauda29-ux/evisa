# Railway Side Panel v5

This build exposes `/api/device/jobs` and reports `deviceJobs: true` and `build: railway-sidepanel-v5` from `/api/health`.

If the extension reports that Railway is still running the older backend, upload this build to the GitHub repository connected to Railway and wait for the new deployment to complete.
